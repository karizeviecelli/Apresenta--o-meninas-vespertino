[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[user]
	name = karizeviecelli
	email = karizeviecellisc@gmail.com
[credential]
	helper = manager
